# fly.toml app configuration file for Attune / Clynotion
# https://fly.io/docs/reference/configuration/

app = 'clynotion'
primary_region = 'iad'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  # One warm machine; volume-backed roster/tokens need a single writer.
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '15s'
    timeout = '5s'
    grace_period = '30s'
    method = 'GET'
    path = '/health'

# Durable roster + Workspace directory tokens (create with deploy workflow).
[[mounts]]
  source = 'attune_data'
  destination = '/data'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
